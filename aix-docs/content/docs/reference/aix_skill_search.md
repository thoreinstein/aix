---
title: "aix skill search"
description: "Reference for aix skill search command"
draft: false
toc: true
---
## aix skill search

Search for skills in repositories

### Synopsis

Search for skills across all configured repositories by name or description.

```
aix skill search <query> [flags]
```

### Examples

```
  # Search for skills matching "test"
  aix skill search test

  # Search within a specific repository
  aix skill search test --repo=my-repo

  # Output results as JSON
  aix skill search test --json
```

### Options

```
  -h, --help          help for search
      --json          Output in JSON format
      --repo string   Filter by repository name
```

### Options inherited from parent commands

```
      --log-file string     write logs to file in JSON format
      --log-format string   log format: text, json (default "text")
  -p, --platform strings    target platform(s): claude, opencode (default: all detected)
  -q, --quiet               suppress non-error output
  -v, --verbose count       increase verbosity level (e.g., -v, -vv)
```

### SEE ALSO

* [aix skill](/docs/reference/aix_skill/)	 - Manage skills across platforms

###### Auto generated by spf13/cobra on 1-Feb-2026
