---
title: "aix backup"
description: "Reference for aix backup command"
draft: false
toc: true
---
## aix backup

Manage configuration backups

### Synopsis

Manage configuration backups for AI coding assistant platforms.

Before aix modifies platform configurations, it automatically creates backups.
This command group allows you to list, restore, create, and prune backups.

Backups are stored in ~/.config/aix/backups/ organized by platform.

```
aix backup [flags]
```

### Examples

```
  # List all backups
  aix backup list

  # List backups for a specific platform
  aix backup list --platform claude

  # Restore from the most recent backup
  aix backup restore --platform claude

  # Restore from a specific backup
  aix backup restore 20260123T100712 --platform claude

  # Create a manual backup
  aix backup create --platform claude

  # Remove old backups, keeping the 3 most recent
  aix backup prune --keep 3

  See Also:
    aix backup list    - List available backups
    aix backup restore - Restore from a backup
    aix backup create  - Manually create a backup
    aix backup prune   - Remove old backups
```

### Options

```
  -h, --help   help for backup
```

### Options inherited from parent commands

```
      --log-file string     write logs to file in JSON format
      --log-format string   log format: text, json (default "text")
  -p, --platform strings    target platform(s): claude, opencode (default: all detected)
  -q, --quiet               suppress non-error output
  -v, --verbose count       increase verbosity level (e.g., -v, -vv)
```

### SEE ALSO

* [aix](/docs/reference/aix/)	 - Unified CLI for AI coding assistant configurations
* [aix backup create](/docs/reference/aix_backup_create/)	 - Create a manual backup
* [aix backup list](/docs/reference/aix_backup_list/)	 - List available backups
* [aix backup prune](/docs/reference/aix_backup_prune/)	 - Remove old backups
* [aix backup restore](/docs/reference/aix_backup_restore/)	 - Restore from a backup

###### Auto generated by spf13/cobra on 3-Feb-2026
