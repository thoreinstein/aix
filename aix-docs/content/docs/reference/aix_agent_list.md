---
title: "aix agent list"
description: "Reference for aix agent list command"
draft: false
toc: true
---
## aix agent list

List installed agents

### Synopsis

List all installed agents grouped by platform.

By default, lists agents for all detected platforms. Use the --platform flag
to limit to specific platforms.

Examples:
  # List all agents
  aix agent list

  # List agents for a specific platform
  aix agent list --platform claude

  # Output as JSON
  aix agent list --json

```
aix agent list [flags]
```

### Options

```
  -h, --help           help for list
      --json           Output in JSON format
      --scope string   configuration scope (user, project, local)
```

### Options inherited from parent commands

```
      --log-file string     write logs to file in JSON format
      --log-format string   log format: text, json (default "text")
  -p, --platform strings    target platform(s): claude, opencode (default: all detected)
  -q, --quiet               suppress non-error output
  -v, --verbose count       increase verbosity level (e.g., -v, -vv)
```

### SEE ALSO

* [aix agent](/docs/reference/aix_agent/)	 - Manage AI coding agents

###### Auto generated by spf13/cobra on 3-Feb-2026
